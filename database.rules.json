{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null",
        "meta": {
          ".validate": "newData.hasChildren(['roomName','passwordHash','ownerUid','createdAt'])"
        },
        "filesMeta": {
          "$fileId": {
            ".validate": "newData.hasChildren(['name','size','contentType','createdAt','chunkCount'])"
          }
        },
        "fileData": {
          "$fileId": {
            "$chunk": {
              ".validate": "newData.isString()"
            }
          }
        }
      }
    }
  }
}